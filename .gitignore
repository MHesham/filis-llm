# Secrets — never commit
.env
admin-credentials.txt

# Internal security/privacy audit — file paths, permissions, bypass mechanisms.
# Publishing this is a roadmap for unauthorized access. See its own header.
PRIVACY-INTERNAL.md

# Open WebUI database (users, chats)
data/

# Model weights live outside the repo, but guard against copies
models/
*.safetensors
*.gguf

# Local scratch
*.log
*.tmp
.DS_Store
